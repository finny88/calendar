{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "7.2.0"
  },
  "dependencies": {},
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "serve": "lerna run serve",
    "check": "lerna run check"
  }
}
